// Main-game release policy. Four-player bindings and simulation remain compatible.
export const LOCAL_HUMAN_PLAYERS = ["A", "B"] as const;
export const LOCAL_HUMAN_LIMIT = LOCAL_HUMAN_PLAYERS.length;
