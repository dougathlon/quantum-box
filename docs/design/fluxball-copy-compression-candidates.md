# Fluxball copy compression candidates

Recorded during Stage 4; intentionally not removed without a visual review:

- verbose shared rule-change wording;
- full goal sentences;
- repeated player letters beside already-positioned goal cards;
- permanent control legends after the ready phase;
- `NEXT ROUND · SPACE`.

The current revision removes the previous-rule cards from Individual Fluxball and replaces per-human reveal tokens with one shared CHANGE RULES status. The remaining candidates still require visual review.
